services:
  laravel:
    build:
      context: ./laravel
    depends_on:
      - mysql
    volumes:
      - ./laravel:/var/www/html
    environment:
      MYSQL_HOST: mysql
      MYSQL_DATABASE: laravel_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: Admin123

  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
    depends_on:
      - laravel

  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: Admin123
      MYSQL_DATABASE: laravel_db
      MYSQL_USER: admin
      MYSQL_PASSWORD: Admin123

  random_http:
    build:
      context: ./random_http
    ports:
      - "8080:80"
